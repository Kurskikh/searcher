# 📁 Структура проекта

## Основные файлы

```
I:\AI\Searcher\
│
├── 🐍 file_searcher.py          # Главный файл приложения
│   ├── FileSearchEngine          # Ядро поисковой системы
│   │   ├── Многопоточный поиск
│   │   ├── Memory-mapped I/O
│   │   └── Regex обработка
│   └── FileSearcherApp           # GUI приложение
│       ├── CustomTkinter интерфейс
│       ├── Обработка событий
│       └── Экспорт результатов
│
├── 📦 requirements.txt           # Python зависимости
│   ├── customtkinter >= 5.2.0
│   └── Pillow >= 10.0.0
│
├── 🚀 start.bat                  # Быстрый запуск (Windows)
├── ⚙️ install.bat                # Установка зависимостей (Windows)
│
├── 📖 README.md                  # Полная документация
├── 🎯 QUICK_START.md            # Быстрый старт
├── 🔥 ADVANCED.md               # Расширенное использование
├── 📋 PROJECT_STRUCTURE.md      # Этот файл
│
└── ⚙️ search_presets.json       # 30+ готовых пресетов поиска
```

---

## Детальная архитектура

### 🎯 file_searcher.py

#### Класс: `SearchResult` (dataclass)
```
📝 Описание: Структура данных для хранения результата поиска
📊 Поля:
  - path: str          # Полный путь к файлу
  - size: int          # Размер в байтах
  - modified: datetime # Дата модификации
  - match_reason: str  # Причина совпадения
```

#### Класс: `FileSearchEngine`
```
⚙️ Описание: Многопоточный движок поиска файлов
🔧 Параметры:
  - max_workers: int   # Количество потоков

📚 Методы:
  ┣━ search()           # Основной метод поиска
  ┃   ├─ Параметры поиска (имя, размер, дата, содержимое)
  ┃   ├─ Многопоточная обработка
  ┃   └─ Callback для прогресса
  ┃
  ┣━ _check_file()      # Проверка одного файла
  ┃   ├─ Валидация имени (wildcard/regex)
  ┃   ├─ Проверка расширения
  ┃   ├─ Фильтрация по размеру
  ┃   ├─ Фильтрация по дате
  ┃   └─ Поиск в содержимом
  ┃
  ┣━ _search_in_file()  # Быстрый поиск в файле
  ┃   ├─ Проверка типа файла
  ┃   ├─ Memory-mapped I/O
  ┃   └─ UTF-8 декодирование
  ┃
  └━ stop()             # Остановка поиска
```

#### Класс: `FileSearcherApp` (CustomTkinter GUI)
```
🖥️ Описание: Главное окно приложения
📐 Размер: 1400x900 px
🎨 Тема: Dark mode

🧩 Компоненты:
  ┣━ Левая панель (400px)
  ┃   ┣━ Директория поиска
  ┃   ┣━ Имя файла (wildcard/regex)
  ┃   ┣━ Расширения
  ┃   ┣━ Поиск в содержимом
  ┃   ┣━ Размер файла (мин/макс)
  ┃   ┣━ Количество потоков
  ┃   ┣━ Кнопки управления
  ┃   ┗━ Прогресс-бар
  ┃
  ┗━ Правая панель (расширяемая)
      ┣━ Заголовок результатов
      ┣━ Кнопки экспорта (CSV/JSON)
      ┗━ Текстовое поле результатов

📚 Основные методы:
  ┣━ _create_widgets()        # Создание интерфейса
  ┣━ _start_search()          # Запуск поиска
  ┣━ _search_worker()         # Рабочий поток
  ┣━ _search_callback()       # Обновление прогресса
  ┣━ _add_result()            # Добавление результата
  ┣━ _search_complete()       # Завершение поиска
  ┣━ _stop_search()           # Остановка поиска
  ┣━ _clear_results()         # Очистка результатов
  ┣━ _export_to_csv()         # Экспорт в CSV
  ┣━ _export_to_json()        # Экспорт в JSON
  ┗━ _format_size()           # Форматирование размера
```

---

## Поток выполнения

### 1. Запуск приложения
```
start.bat
  └─> Проверка Python
  └─> Проверка зависимостей
  └─> python file_searcher.py
      └─> FileSearcherApp.__init__()
          └─> _create_widgets()
          └─> mainloop()
```

### 2. Процесс поиска
```
Пользователь нажимает "НАЧАТЬ ПОИСК"
  ↓
_start_search()
  ├─> Валидация параметров
  ├─> Создание FileSearchEngine
  └─> Запуск _search_worker() в отдельном потоке
      ↓
_search_worker()
  └─> FileSearchEngine.search()
      ├─> os.walk() для обхода директорий
      ├─> ThreadPoolExecutor для параллелизма
      │   ├─> Поток 1: _check_file(file1)
      │   ├─> Поток 2: _check_file(file2)
      │   ├─> Поток 3: _check_file(file3)
      │   └─> Поток N: _check_file(fileN)
      │
      └─> Для каждого найденного файла:
          └─> callback() → _search_callback()
              └─> _add_result() (в главном потоке GUI)
                  └─> Обновление интерфейса
  ↓
_search_complete()
  └─> Финальная статистика
```

### 3. Экспорт результатов
```
Пользователь нажимает "Экспорт в CSV/JSON"
  ↓
_export_results(format_type)
  ├─> filedialog.asksaveasfilename()
  └─> _export_to_csv() / _export_to_json()
      └─> Запись файла
      └─> messagebox.showinfo()
```

---

## Технические детали

### Многопоточность
```
ThreadPoolExecutor
  ├─ max_workers = настраиваемо (по умолчанию = CPU cores)
  ├─ Каждый поток обрабатывает отдельные файлы
  └─ as_completed() для получения результатов по мере готовности
```

### Memory-mapped I/O
```
Для файлов > 8192 байт:
  open(file, 'rb') → mmap.mmap(ACCESS_READ)
    └─> Быстрое чтение без загрузки всего файла в память
    └─> Автоматическое управление памятью ОС
```

### Regex обработка
```
Компиляция паттернов:
  re.compile(pattern, flags)
    ├─ Кэширование скомпилированного regex
    └─ Флаги: IGNORECASE (опционально)

Применение:
  pattern.search(text) → Match объект или None
```

---

## Файлы данных

### requirements.txt
```
customtkinter>=5.2.0   # Современный GUI фреймворк
Pillow>=10.0.0         # Обработка изображений (зависимость CTk)
```

### search_presets.json
```json
{
  "presets": [
    {
      "name": "Имя пресета",
      "description": "Описание",
      "directory": "Путь",
      "name_pattern": "Паттерн имени",
      "extensions": "ext1, ext2",
      "content_regex": "Regex для содержимого",
      ...
    }
  ]
}
```

---

## Расширения и модификации

### Добавление новой функции поиска

1. **Добавить UI элемент** в `_create_widgets()`:
```python
self.new_filter = ctk.CTkEntry(...)
```

2. **Передать параметр** в `_start_search()`:
```python
new_param = self.new_filter.get()
```

3. **Использовать в** `FileSearchEngine.search()`:
```python
def search(self, ..., new_param=None):
    # Реализация
```

4. **Применить фильтр** в `_check_file()`:
```python
if new_param and not check_condition(file, new_param):
    return None
```

### Добавление нового формата экспорта

1. **Добавить кнопку** в интерфейс
2. **Создать метод** `_export_to_format()`:
```python
def _export_to_xml(self, file_path):
    # Генерация XML
    with open(file_path, 'w') as f:
        f.write(xml_content)
```

---

## Оптимизация производительности

### Узкие места:
1. **I/O операции** - чтение файлов с диска
   - Решение: Многопоточность, SSD
   
2. **Regex обработка** - сложные паттерны
   - Решение: Оптимизация regex, ограничение размера файлов
   
3. **GUI обновления** - частые обновления интерфейса
   - Решение: Батчинг (обновление раз в 100 файлов)

### Оптимальные настройки:
```
SSD диск:     CPU cores × 2-4 потока
HDD диск:     CPU cores × 1 поток
Сетевой диск: 1-2 потока
```

---

## Безопасность

### Обработка ошибок:
- ✅ PermissionError - игнорируется
- ✅ OSError - игнорируется
- ✅ UnicodeDecodeError - игнорируется
- ✅ ValueError - сообщение пользователю
- ✅ re.error - валидация regex

### Ограничения:
- Максимальный размер файла для поиска в содержимом: 100 МБ
- Пропуск бинарных файлов (содержат \x00)
- Пропуск скрытых директорий (.git, .venv)

---

## Зависимости

### Python модули (встроенные):
```
os              # Работа с файловой системой
re              # Регулярные выражения
mmap            # Memory-mapped файлы
threading       # Многопоточность
pathlib         # Пути к файлам
datetime        # Работа с датами
json            # JSON сериализация
csv             # CSV экспорт
fnmatch         # Wildcard паттерны
dataclasses     # Структуры данных
concurrent.futures  # ThreadPoolExecutor
tkinter         # Базовый GUI (зависимость CustomTkinter)
```

### Внешние библиотеки:
```
customtkinter   # Современный GUI
Pillow          # Обработка изображений
```

---

## Тестирование

### Ручное тестирование:
1. Создать тестовую директорию с файлами
2. Проверить различные фильтры
3. Проверить поиск по содержимому
4. Проверить экспорт результатов
5. Проверить остановку поиска

### Типичные тестовые сценарии:
- ✅ Пустая директория
- ✅ Очень большая директория (>10000 файлов)
- ✅ Файлы без расширения
- ✅ Файлы с unicode именами
- ✅ Большие файлы (>1 ГБ)
- ✅ Недоступные файлы (permission denied)

---

## Производительность

### Benchmarks (примерные):
```
Задача: Поиск по имени в 10,000 файлов
  - 1 поток:   ~3 сек
  - 4 потока:  ~1 сек
  - 8 потоков: ~0.6 сек

Задача: Поиск по содержимому в 1,000 файлов (по 10 КБ)
  - 1 поток:   ~8 сек
  - 4 потока:  ~3 сек
  - 8 потоков: ~2 сек
```

---

## Лицензия и авторство

- **Лицензия**: Свободное использование
- **Технологии**: Python 3.8+, CustomTkinter
- **Платформа**: Windows (может работать на Linux/Mac с модификациями)

---

**Удачной работы с проектом! 🚀**

